<div class="container">
<%= form_for [@post, @post.comments.build], remote: true do |f| %>
  <%= f.label :comment %>
	<%= f.text_field :comment, class: "form-control"  %>
  <br />
	<%= f.submit %>
<% end %>
</div>
