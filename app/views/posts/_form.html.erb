<div class="container">
	<%= form_for @post do |f| %>
	<%= f.label :title %>
  <%= f.text_field :title, class: "form-control"  %>
	<%= f.label :body %>
  <%= f.text_area :body, class: "form-control"  %>
	<br />
  <%= f.submit %>
  <% end %>
</div>
